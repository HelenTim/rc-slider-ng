import Slider from './Slider';
import type { SliderProps } from './Slider';
import TooltipSlider, { handleRender } from './TooltipSlider';

export type { SliderProps };

export { TooltipSlider, handleRender };
export default Slider;
